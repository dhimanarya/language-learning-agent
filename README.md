# language-learning-agent
A ReAct agent built in LangGraph which allows people to select random words from pre-defined word lists, translate them into another language, and create Anki cards containing the words and translations.
